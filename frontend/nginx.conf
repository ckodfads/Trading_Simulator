server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Lệnh này khắc phục lỗi 404 của React Router
        try_files $uri $uri/ /index.html; 
    }
}
